# Lessons: Deployment

### From mission `deploy-cv-it1st` (project: cv-quicklinks)

- **[deployment]** When git auth fails on remote server, use tar pipe instead of SCP — *scout-deploy*
- **[deployment]** git credential cache used wrong GitHub account — run 'gh auth setup-git' first — *scout-deploy*
- **[deployment]** Nginx on hummingbot serves multiple sites — just add a new server block in sites-enabled — *scout-deploy*

### From mission `deploy-crypto-dashboard` (project: crypto-income)

- **[deployment]** Reuse: already know to use tar pipe + gh auth setup-git (from deploy-cv-it1st) — *general-deploy*
- **[deployment]** PM2 is the simplest process manager for Node.js apps on hummingbot — *general-deploy*
