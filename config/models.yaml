# Model Assignment Matrix
# RULE: Never downgrade. Only upgrade with proof.
# RULE: Free models only. We make money, not spend it.

models:
  # Tier 1: Best available free
  primary:
    id: xiaomi/mimo-v2-omni:free
    provider: kilocode
    capabilities: [text, vision, tools, reasoning]
    context: 262144
    cost: 0
    notes: ONLY free model with vision + tools

  # Tier 2: Strong text-only free
  fallback_text:
    - id: xiaomi/mimo-v2-pro:free
      provider: kilocode
      capabilities: [text, tools, reasoning]
      context: 1048576
      cost: 0
      notes: 1M context, no vision

    - id: stepfun/step-3.5-flash:free
      provider: kilocode
      capabilities: [text, tools]
      context: 262144
      cost: 0

    - id: z-ai/glm-4.5-air:free
      provider: openrouter
      capabilities: [text, tools, reasoning]
      context: 131072
      cost: 0

  # Tier 3: Paid fallback (last resort)
  fallback_paid:
    - id: github-copilot/gpt-5-mini
      provider: github-copilot
      capabilities: [text, vision, tools, reasoning]
      cost: paid
      notes: Last resort. Use sparingly.

# Task type → model mapping
task_matrix:
  vision:
    primary: xiaomi/mimo-v2-omni:free
    fallback: github-copilot/gpt-5-mini

  code:
    primary: xiaomi/mimo-v2-omni:free
    fallback: xiaomi/mimo-v2-pro:free

  reasoning:
    primary: xiaomi/mimo-v2-omni:free
    fallback: z-ai/glm-4.5-air:free

  creative:
    primary: xiaomi/mimo-v2-omni:free
    fallback: xiaomi/mimo-v2-pro:free

  research:
    primary: stepfun/step-3.5-flash:free
    fallback: xiaomi/mimo-v2-pro:free

  web_scraping:
    primary: xiaomi/mimo-v2-omni:free
    fallback: xiaomi/mimo-v2-pro:free
